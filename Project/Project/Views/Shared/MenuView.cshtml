
@model IEnumerable<Project.Models.Loai>
@*<div class="collapse navbar-collapse" id="navbarsupportedcontent">
        <div class="d-flex flex-column flex-lg-row align-items-center">
            <ul class="navbar-nav ">
                <li class="nav-item">
                    <a class="nav-link" href="/Home/store">Home </a>
                </li>
                @foreach (var item in Model)
                {
                    <li class="nav-item">
                        <a class="nav-link" href="/danhmuc/index/@item.maloai">@item.tenloai </a>
                    </li>
                }
                @if (Session["taikhoan"] == null)
                {
                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("login","users")">login</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("register","users")">register</a>
                    </li>
                }
                <li class="nav-item">
                    <a class="nav-link" href="@Url.Action("giohang","giohang")">
                        <i class="fa fa-shopping-bag shoppingbag"></i>
                    </a>
                </li>
            </ul>


            <form class="form-inline my-2 my-lg-0 ml-0 ml-lg-4 mb-3mb-lg-0">
                <button class="btn my-2 my-sm-0 nav_search-btn" type="submit"></button>
            </form>
        </div>
    </div>*@
    <nav class="nav-bar">
        <ul>
            <li><a href="/Home/store">Home</a></li>
            @foreach (var item in Model)
            {
                <li>
                    <a href="/danhmuc/index/@item.maloai">@item.tenloai </a>
                </li>
            }
            @if (Session["taikhoan"] == null)
            {
                <li>
                    <a href="@Url.Action("Login","Users")">login</a>
                </li>
                <li>
                    <a href="@Url.Action("Register","Users")">login</a>
                </li>
            }
            <li>
                <a href="@Url.Action("giohang","giohang")">
                    <i class="fa fa-shopping-bag shoppingbag"></i>
                </a>
            </li>

        </ul>
    </nav>

<style>
    .nav-bar {
        display: flex;
        justify-content: center;
        align-items: center;
        background-color: #1f1f1f;
        padding:20px;
        position:fixed;
        z-index:999;
        width:100%;
    }

        .nav-bar ul {
            display: flex;
            list-style: none;
            margin: 0;
            padding: 0;
        }

        .nav-bar li {
            margin: 0 10px;
        }

        .nav-bar a {
            color: #fff;
            text-decoration: none;
            font-weight: bold;
            text-transform: uppercase;
            padding: 10px;
            transition: all 0.3s ease-in-out;
        }

            .nav-bar a:hover {
                color: #fff;
                background-color: #333;
            }

</style>